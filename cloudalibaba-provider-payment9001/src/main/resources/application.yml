server:
  port: 9001
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos 地址

#  devtools:
#    add-properties: false
management:
  endpoints:
    web:
      exposure:
        include: "*"
